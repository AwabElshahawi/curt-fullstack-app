$kind: http-request
name: Get project by id - invalid id format (expect 400/500)
url: "{{baseUrl}}/projects/not-a-valid-id"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Does not crash unhandled', () => pm.expect(pm.response.code).to.be.oneOf([400, 404, 500]));
    language: text/javascript
order: 7000
